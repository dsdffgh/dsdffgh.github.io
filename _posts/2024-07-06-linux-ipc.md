---
title: "进程间通信"
date: 2024-07-06
categories: [linux, kernel]
tags: ["IPC", "process"]
---
为了保证安全，每个进程的用户地址空间都是独立的，一般而言一个进程不能直接访问另一个进程的地址空间，不过内核空间是每个进程都共享的，所以**进程之间想要进行信息交换就必须通过内核**。

---

### 管道

无格式的字节流数据

[https://github.com/0voice/linux_kernel_wiki/blob/main/文章/进程管理/进程间通信之管道.md](https://github.com/0voice/linux_kernel_wiki/blob/main/%E6%96%87%E7%AB%A0/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1%E4%B9%8B%E7%AE%A1%E9%81%93.md)

#### 匿名管道

生命周期，是随进程的创建而建立，随进程的结束而销毁

**通信范围是存在父子关系的进程**。因为管道没有实体，也就是没有管道文件，只能通过 fork 来复制父进程 fd 文件描述符，来达到通信的目的。

```c
A|B
```

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-01.webp)

匿名管道的创建，需要通过下面这个系统调用：

`int pipe(int fd[2])`

这里表示创建一个匿名管道，并返回了两个描述符，一个是管道的读取端描述符 `fd[0]`，另一个是管道的写入端描述符 `fd[1]`。注意，这个匿名管道是特殊的文件，只存在于内存，不存于文件系统中。

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-02.webp)

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-03.webp)

#### 命名管道`FIFO`

```c
$ echo "hello" > myPipe  
$ cat < myPipe  // 读取管道里的数据
hello
```

不管是匿名管道还是命名管道，进程写入的数据都是缓存在内核中，另一个进程读取数据时候自然也是从内核中获取，同时通信数据都遵循**先进先出**原则，不支持 lseek 之类的文件定位操作。

### 消息队列

一种保存在内核中的消息链表，发送方和接收方约定好消息体的数据类型，每个消息体都是固定大小的存储块。生命周期随内核
不足的地方有两点，**一是通信不及时，二是附件也有大小限制**，这同样也是消息队列通信不足的点。
存在用户态到内核态的数据拷贝开销。因为进程写入和读取都要进入内核态

### 共享内存

**共享内存的机制，就是拿出一块虚拟地址空间来，映射到相同的物理内存中**。这样这个进程写入的东西，另外一个进程马上就能看到了，都不需要拷贝来拷贝去，传来传去，大大提高了进程间通信的速度。
新的问题：竞争。如果多个进程同时修改同一个共享内存，很有可能就冲突了。例如两个进程都同时写一个地址，那先写的那个进程会发现内容被别人覆盖了。
可以使用信号量来解决。信号量在枷锁时间很长或者锁持有者可能睡眠的情况下使用。

有 `down()` 和 `up()` 两种操作

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-04.webp)

### 异常状态下的通信

**用「信号」的方式来通知进程。**

信号跟信号量虽然名字相似度 66.66%，但两者用途完全不一样，就好像 Java 和 JavaScript 的区别。

信号事件的来源主要有硬件来源（如键盘 Cltr+C ）和软件来源（如 kill 命令）。

信号是进程间通信机制中**唯一的异步通信机制**，因为可以在任何时候发送信号给某一进程，一旦有信号产生，我们就有下面这几种，用户进程对信号的处理方式。

**1.执行默认操作**。Linux 对每种信号都规定了默认操作，例如，上面列表中的 SIGTERM 信号，就是终止进程的意思。

**2.捕捉信号**。我们可以为信号定义一个信号处理函数。当信号发生时，我们就执行相应的信号处理函数。

**3.忽略信号**。当我们不希望处理某些信号的时候，就可以忽略该信号，不做任何处理。有两个信号是应用进程无法捕捉和忽略的，即 `SIGKILL` 和 `SEGSTOP`，它们用于在任何时候中断或结束某一进程。

### **Socket套接字**

不仅可以跨网络与不同主机的进程间通信，还可以在同主机上进程间通信。
是应用层与 TCP/IP 协议族通信的中间软件抽象层，它对 TCP/IP 进行了封装。它**把复杂的 TCP/IP 协议族隐藏在 Socket 接口后面**。对用户来说，只要通过一组简单的 API 就可以实现网络的连接。

创建 socket 的系统调用：

```c
int socket(int domain, int type, int protocal)
```

`domain` 指定协议簇， 比如 AF_INET 用于 IPV4、AF_INET6 用于 IPV6、AF_LOCAL/AF_UNIX 用于本机；

`type` 指定通信特性，比如 SOCK_STREAM 表示的是字节流，对应 TCP、SOCK_DGRAM 表示的是数据报，对应 UDP、SOCK_RAW 表示的是原始套接字；

`protocal` protocol 目前一般写成 0 即可；

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-05.webp)

- 服务端和客户端初始化 `socket`，得到文件描述符；
- 服务端调用 `bind`，将绑定在 IP 地址和端口;
- 服务端调用 `listen`，进行监听；
- 服务端调用 `accept`，等待客户端连接；
- 客户端调用 `connect`，向服务器端的地址和端口发起连接请求；
- 服务端 `accept` 返回用于传输的 `socket` 的文件描述符；
- 客户端调用 `write` 写入数据；服务端调用 `read` 读取数据；
- 客户端断开连接时，会调用 `close`，那么服务端 `read` 读取数据的时候，就会读取到了 `EOF`，待处理完数据后，服务端调用 `close`，表示连接关闭。

服务端调用 `accept` 时，连接成功了会返回一个已完成连接的 socket，后续用来传输数据。
所以，监听的 socket 和真正用来传送数据的 socket，是「**两个**」 socket，一个叫作**监听 socket**，一个叫作**已完成连接 socket**。

#### 针对 UDP 协议通信的 socket 编程模型

UDP 是没有连接的，所以不需要三次握手，也就不需要像 TCP 调用 listen 和 connect，但是 UDP 的交互仍然需要 IP 地址和端口号，因此也需要 bind。

对于 UDP 来说，不需要要维护连接，那么也就没有所谓的发送方和接收方，甚至都不存在客户端和服务端的概念，只要有一个 socket 多台机器就可以任意通信，因此每一个 UDP 的 socket 都需要 bind。

另外，每次通信时，调用 sendto 和 recvfrom，都要传入目标主机的 IP 地址和端口。

![Untitled](/assets/images/linux-kernel-notes/linux-ipc/image-06.webp)

#### 针对本地进程间通信的 socket 编程模型

本地 socket  被用于在**同一台主机上进程间通信**的场景：

- 本地 socket 的编程接口和 IPv4 、IPv6 套接字编程接口是一致的，可以支持「字节流」和「数据报」两种协议；
- 本地 socket 的实现效率大大高于 IPv4 和 IPv6 的字节流、数据报 socket 实现；

对于本地字节流 socket，其 socket 类型是 AF_LOCAL 和 SOCK_STREAM。

对于本地数据报 socket，其 socket 类型是 AF_LOCAL 和 SOCK_DGRAM。

本地字节流 socket 和 本地数据报 socket 在 bind 的时候，不像 TCP 和 UDP 要绑定 IP 地址和端口，而是**绑定一个本地文件**，这也就是它们之间的最大区别。

---

[造成socket.error: [Errno 99] Cannot assign requested - djiankuo - 博客园](https://www.cnblogs.com/djiankuo/p/5956606.html)

[ma35d1 yocto 编译环境搭建 - 大大通(简体站)](https://www.wpgdadatong.com.cn/blog/detail/71223)

事情起因是为 ma35d1 构建 yocto 环境， 为了方便，不想花功夫配环境，就[用了docker，根据](https://www.wpgdadatong.com.cn/blog/detail/71223)

```bash
wheeltec-client@ff599ee1a03c:~/shared/ma35d1-yocto$ repo init -u https://github.com/OpenNuvoton/MA35D1_Yocto-v3.1.3.git -m meta-ma35d1/base/ma35d1.xml
Downloading Repo source from https://gerrit.googlesource.com/git-repo
fatal: Cannot get https://gerrit.googlesource.com/git-repo/clone.bundle
fatal: error [Errno 99] Cannot assign requested address
fatal: double check your --repo-rev setting.
fatal: cloning the git-repo repository failed, will remove '.repo/repo' 
wheeltec-client@ff599ee1a03c:~/shared/ma35d1-yocto$
```

这里repo init的时候报了99错误。于是根据 [博文](https://www.cnblogs.com/djiankuo/p/5956606.html) 想到查看socket的调用

```bash
wheeltec-client@ubuntu:~/ma35d1-yocto$ strace -e trace=connect -f repo init -u https://github.com/OpenNuvoton/MA35D1_Yocto-v3.1.3.git -m meta-ma35d1/base/ma35d1.xml
```

输出结果为

```bash
wheeltec-client@ubuntu:~/ma35d1-yocto$ strace -e trace=connect -f repo init -u https://github.com/OpenNuvoton/MA35D1_Yocto-v3.1.3.git -m meta-ma35d1/base/ma35d1.xml
strace: Process 86298 attached
[pid 86298] +++ exited with 0 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86298, si_uid=1000, si_status=0, si_utime=0, si_stime=0} ---
Downloading Repo source from https://gerrit.googlesource.com/git-repo
strace: Process 86299 attached
[pid 86299] +++ exited with 0 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86299, si_uid=1000, si_status=0, si_utime=0, si_stime=0} ---
strace: Process 86300 attached
[pid 86300] +++ exited with 0 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86300, si_uid=1000, si_status=0, si_utime=0, si_stime=0} ---
strace: Process 86301 attached
[pid 86301] +++ exited with 0 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86301, si_uid=1000, si_status=0, si_utime=0, si_stime=0} ---
strace: Process 86302 attached
[pid 86302] +++ exited with 1 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86302, si_uid=1000, si_status=1, si_utime=0, si_stime=0} ---
connect(6, {sa_family=AF_INET, sin_port=htons(52970), sin_addr=inet_addr("10.107.132.173")}, 16) = 0
connect(6, {sa_family=AF_INET, sin_port=htons(52970), sin_addr=inet_addr("10.107.132.173")}, 16) = 0
strace: Process 86303 attached
strace: Process 86304 attached
[pid 86304] +++ exited with 128 +++
[pid 86303] --- SIGPIPE {si_signo=SIGPIPE, si_code=SI_USER, si_pid=86303, si_uid=1000} ---
[pid 86303] --- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86304, si_uid=1000, si_status=128, si_utime=0, si_stime=1} ---
strace: Process 86305 attached
strace: Process 86306 attached
strace: Process 86307 attached
[pid 86306] +++ exited with 0 +++
[pid 86307] +++ exited with 0 +++
[pid 86305] +++ exited with 0 +++
[pid 86303] --- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86305, si_uid=1000, si_status=0, si_utime=42, si_stime=32} ---
strace: Process 86308 attached
[pid 86308] +++ exited with 0 +++
[pid 86303] --- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86308, si_uid=1000, si_status=0, si_utime=2, si_stime=1} ---
[pid 86303] connect(5, {sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"}, 110) = -1 ENOENT (No such file or directory)
[pid 86303] connect(5, {sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"}, 110) = -1 ENOENT (No such file or directory)
[pid 86303] connect(5, {sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"}, 110) = -1 ENOENT (No such file or directory)
[pid 86303] connect(5, {sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"}, 110) = -1 ENOENT (No such file or directory)
strace: Process 86309 attached
[pid 86309] +++ exited with 0 +++
[pid 86303] --- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86309, si_uid=1000, si_status=0, si_utime=0, si_stime=0} ---
[pid 86303] +++ exited with 0 +++
--- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86303, si_uid=1000, si_status=0, si_utime=4, si_stime=38} ---
strace: Process 86310 attached
strace: Process 86311 attached
[pid 86311] connect(3, {sa_family=AF_INET, sin_port=htons(52970), sin_addr=inet_addr("10.107.132.173")}, 16) = -1 EINPROGRESS (Operation now in progress)
fatal: unable to access 'https://gerrit.googlesource.com/git-repo/': gnutls_handshake() failed: The TLS connection was non-properly terminated.
[pid 86310] +++ exited with 128 +++
[pid 86297] --- SIGCHLD {si_signo=SIGCHLD, si_code=CLD_EXITED, si_pid=86310, si_uid=1000, si_status=128, si_utime=0, si_stime=2} ---
repo: error: "git" failed with exit status 128
  cwd: /home/wheeltec-client/ma35d1-yocto/.repo/repo.tmp
  cmd: ['git', 'fetch', '--quiet', '--progress', 'origin', '+refs/heads/*:refs/remotes/origin/*', '+refs/tags/*:refs/tags/*'][pid 86311] +++ exited with 128 +++

fatal: double check your --repo-rev setting.
fatal: cloning the git-repo repository failed, will remove '.repo/repo' 
+++ exited with 1 +++
wheeltec-client@ubuntu:~/ma35d1-yocto$ 
```

这里由chatGPT逐行解释发生的情况：

1. **strace命令启动和退出信息**：
    - `strace: Process 86298 attached`：表示strace成功启动，并监视进程号为86298的子进程。
    - `+++ exited with 0 +++`：进程号86298正常退出，退出状态码0。
2. **repo init命令执行尝试**：
    - `Downloading Repo source from <https://gerrit.googlesource.com/git-repo`：repo命令尝试从指定URL下载git-repo。>
    - `strace: Process 86299 attached`至`strace: Process 86301 attached`：每个进程都成功启动并正常退出。
3. **连接情况**：
    - `connect(6, {sa_family=AF_INET, sin_port=htons(52970), sin_addr=inet_addr("10.107.132.173")}, 16) = 0`：显示成功与目标IP地址（10.107.132.173）和端口（52970）建立连接。
    - `connect(5, {sa_family=AF_UNIX, sun_path="/var/run/nscd/socket"}, 110) = -1 ENOENT (No such file or directory)`：尝试连接本地UNIX域套接字文件失败，文件不存在。
4. **TLS握手失败**：
    - `fatal: unable to access '<https://gerrit.googlesource.com/git-repo/>': gnutls_handshake() failed: The TLS connection was non-properly terminated.`：[TLS握手](TLS%E5%8D%8F%E8%AE%AE%20582298e6966f4ddebc7bda8c9955f955.md)过程中出现错误，连接未能正确终止。
5. **Git命令执行失败**：
    - `repo: error: "git" failed with exit status 128`：使用git命令失败，退出状态码为128。
6. **错误信息**：
    - `fatal: double check your --repo-rev setting.`：提示检查`-repo-rev`参数设置。
    - `fatal: cloning the git-repo repository failed, will remove '.repo/repo'`：克隆git-repo仓库失败，并删除`.repo/repo`目录。

综上所述，问题出现在TLS握手失败，这可能是由于网络或代理设置问题导致的。您可以尝试以下步骤解决问题：

- **确认代理设置**：确保代理设置正确，并且可以在命令行中正确访问HTTPS网址。
- **检查网络连接**：确保网络连接稳定，没有阻止对指定域的访问。
- **尝试直接连接**：在不使用代理的情况下尝试直接连接。

**注意 `AF_INET`表明使用的是 IPv4 (`AF_INET`) 协议，端口号为 `52970`，IP 地址为 `10.107.132.173`。所以在下面查看 `sk->sk_prot->connect` 的具体实现的时候是`tcp_v4_connect`。**

---

```c
socket.c	net	83566	2020/7/1	2099
/**
 *  struct socket - general BSD socket
 *  @state: socket state (%SS_CONNECTED, etc)
 *  @type: socket type (%SOCK_STREAM, etc)
 *  @flags: socket flags (%SOCK_NOSPACE, etc)
 *  @ops: protocol specific socket operations
 *  @file: File back pointer for gc
 *  @sk: internal networking protocol agnostic socket representation
 *  @wq: wait queue for several uses
 */
struct socket {
	socket_state		state;

	kmemcheck_bitfield_begin(type);
	short			type;
	kmemcheck_bitfield_end(type);

	unsigned long		flags;

	struct socket_wq __rcu	*wq;

	struct file		*file;
	struct sock		*sk;
	const struct proto_ops	*ops;
};
```

这里是 socket的定义

根据调用栈

```c
Sys_connect--->
    sock->ops->connect                   // inet_stream_connect
        sk->sk_prot->connect               // tcp_v4_connect
```

```c
arch\x86\entry\syscalls\syscall_64.tbl
42	common	connect			sys_connect
```

```c

socket.c	net	83566	2020/7/1	2099
/*
 *	Attempt to connect to a socket with the server address.  The address
 *	is in user space so we verify it is OK and move it to kernel space.
 *
 *	For 1003.1g we need to add clean support for a bind to AF_UNSPEC to
 *	break bindings
 *
 *	NOTE: 1003.1g draft 6.3 is broken with respect to AX.25/NetROM and
 *	other SEQPACKET protocols that take time to connect() as it doesn't
 *	include the -EINPROGRESS status for such sockets.
 */

SYSCALL_DEFINE3(connect, int, fd, struct sockaddr __user *, uservaddr,
		int, addrlen)
{
	struct socket *sock;
	struct sockaddr_storage address;
	int err, fput_needed;

...
	err =
	    security_socket_connect(sock, (struct sockaddr *)&address, addrlen);
	if (err)
		goto out_put;

	**err = sock->ops->connect(sock, (struct sockaddr *)&address, addrlen,**
				 sock->file->f_flags);
out_put:
	fput_light(sock->file, fput_needed);
out:
	return err;
}
```

`sock->ops->connect` 是指向具体协议实现的函数指针。对于 TCP/IP 协议，通常是 `inet_stream_connect`。

```c
af_inet.c	net\ipv4	48102	2020/7/1	1078
int inet_stream_connect(struct socket *sock, struct sockaddr *uaddr,
			int addr_len, int flags)
{
	int err;

	lock_sock(sock->sk);
	**err = __inet_stream_connect(sock, uaddr, addr_len, flags);**
	release_sock(sock->sk);
	return err;
}
```

```c
af_inet.c	net\ipv4	48102	2020/7/1	1078
/*
 *	Connect to a remote host. There is regrettably still a little
 *	TCP 'magic' in here.
 */
int __inet_stream_connect(struct socket *sock, struct sockaddr *uaddr,
			  int addr_len, int flags)
{
	struct sock *sk = sock->sk;
	int err;
	long timeo;

...
		err = sk->sk_prot->connect(sk, uaddr, addr_len);
		if (err < 0)
			goto out;

		sock->state = SS_CONNECTING;

		/* Just entered SS_CONNECTING state; the only
		 * difference is that return value in non-blocking
		 * case is EINPROGRESS, rather than EALREADY.
		 */
		err = -EINPROGRESS;
		break;
	}
```

而对于这个repo init操作，其 `sk->sk_prot->connect`具体实现为 `tcp_v4_connect` why?

 `repo init` 是一个 Git 工具，它通过 HTTPS 与远程服务器通信。HTTPS 是基于 HTTP 的，HTTP 又是基于 TCP 的。因此，我们可以确定 `repo init` 使用的是 TCP 协议。

对于 `AF_INET`（IPv4）协议族的 `SOCK_STREAM`（TCP）套接字，其 `connect` 操作通常指向 `inet_stream_connect`。`inet_stream_connect` 会调用 `sk->sk_prot->connect`，`sk_prot` 是一个 `struct proto` 结构体，表示具体协议的操作集合。对于 TCP 协议，`sk_prot->connect` 通常指向 `tcp_v4_connect`。如前所述。
接下来看 具体实现

```c
tcp_ipv4.c	net\ipv4	65224	2020/7/1	1591
/* This will initiate an outgoing connection. */
int tcp_v4_connect(struct sock *sk, struct sockaddr *uaddr, int addr_len)
{
	struct sockaddr_in *usin = (struct sockaddr_in *)uaddr;
	struct inet_sock *inet = inet_sk(sk);
	struct tcp_sock *tp = tcp_sk(sk);
	__be16 orig_sport, orig_dport;
	__be32 daddr, nexthop;
	struct flowi4 *fl4;
	struct rtable *rt;
	int err;
	struct ip_options_rcu *inet_opt;

	if (addr_len < sizeof(struct sockaddr_in))
		return -EINVAL;

	if (usin->sin_family != AF_INET)
		return -EAFNOSUPPORT;

	nexthop = daddr = usin->sin_addr.s_addr;
	inet_opt = rcu_dereference_protected(inet->inet_opt,
					     lockdep_sock_is_held(sk));
	if (inet_opt && inet_opt->opt.srr) {
		if (!daddr)
			return -EINVAL;
		nexthop = inet_opt->opt.faddr;
	}

	orig_sport = inet->inet_sport;
	orig_dport = usin->sin_port;
	fl4 = &inet->cork.fl.u.ip4;
	rt = ip_route_connect(fl4, nexthop, inet->inet_saddr,
			      RT_CONN_FLAGS(sk), sk->sk_bound_dev_if,
			      IPPROTO_TCP,
			      orig_sport, orig_dport, sk);
	if (IS_ERR(rt)) {
		err = PTR_ERR(rt);
		if (err == -ENETUNREACH)
			IP_INC_STATS(sock_net(sk), IPSTATS_MIB_OUTNOROUTES);
		return err;
	}

	if (rt->rt_flags & (RTCF_MULTICAST | RTCF_BROADCAST)) {
		ip_rt_put(rt);
		return -ENETUNREACH;
	}

	if (!inet_opt || !inet_opt->opt.srr)
		daddr = fl4->daddr;

	if (!inet->inet_saddr)
		inet->inet_saddr = fl4->saddr;
	sk_rcv_saddr_set(sk, inet->inet_saddr);

	if (tp->rx_opt.ts_recent_stamp && inet->inet_daddr != daddr) {
		/* Reset inherited state */
		tp->rx_opt.ts_recent	   = 0;
		tp->rx_opt.ts_recent_stamp = 0;
		if (likely(!tp->repair))
			tp->write_seq	   = 0;
	}

	if (tcp_death_row.sysctl_tw_recycle &&
	    !tp->rx_opt.ts_recent_stamp && fl4->daddr == daddr)
		tcp_fetch_timewait_stamp(sk, &rt->dst);

	inet->inet_dport = usin->sin_port;
	sk_daddr_set(sk, daddr);

	inet_csk(sk)->icsk_ext_hdr_len = 0;
	if (inet_opt)
		inet_csk(sk)->icsk_ext_hdr_len = inet_opt->opt.optlen;

	tp->rx_opt.mss_clamp = TCP_MSS_DEFAULT;

	/* Socket identity is still unknown (sport may be zero).
	 * However we set state to SYN-SENT and not releasing socket
	 * lock select source port, enter ourselves into the hash tables and
	 * complete initialization after this.
	 */
	tcp_set_state(sk, TCP_SYN_SENT);
	err = inet_hash_connect(&tcp_death_row, sk);
	if (err)
		goto failure;

	sk_set_txhash(sk);

	rt = ip_route_newports(fl4, rt, orig_sport, orig_dport,
			       inet->inet_sport, inet->inet_dport, sk);
	if (IS_ERR(rt)) {
		err = PTR_ERR(rt);
		rt = NULL;
		goto failure;
	}
	/* OK, now commit destination to socket.  */
	sk->sk_gso_type = SKB_GSO_TCPV4;
	sk_setup_caps(sk, &rt->dst);

	if (!tp->write_seq && likely(!tp->repair))
		tp->write_seq = secure_tcp_sequence_number(inet->inet_saddr,
							   inet->inet_daddr,
							   inet->inet_sport,
							   usin->sin_port);

	inet->inet_id = prandom_u32();

	err = tcp_connect(sk);

	rt = NULL;
	if (err)
		goto failure;

	return 0;

failure:
	/*
	 * This unhashes the socket and releases the local port,
	 * if necessary.
	 */
	tcp_set_state(sk, TCP_CLOSE);
	ip_rt_put(rt);
	sk->sk_route_caps = 0;
	inet->inet_dport = 0;
	return err;
}
```

---

源码查看完毕回归正题

解决方法挺简单的，因为在docker里有点慢，就在外部环境里 添加了 git 代理和用户名，，在外部环境 repo init 成功。估计是代理的原因。
